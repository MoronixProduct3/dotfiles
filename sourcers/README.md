The files of this directory are simply copied to the user's `$HOME` directory.
While getting copied, the path of the *dotfiles* repository replaces the
`}}DOTFILES{{` pattern indicating the path of this repository.
