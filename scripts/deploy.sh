#!/bin/sh

# This script is meant to deploy all config files onto a new machine


# Create required directory structure

# Check if git is installed and executable
# Check if it can be installed
    # Install git
    # Clone settings
# Or download settings using curl

# backup old config files

# generate new dotfiles sourcing the ones in the repo
